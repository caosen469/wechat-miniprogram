# 全部数据读写经云函数收口鉴权，不用数据库安全规则

小程序端**不直连云数据库**，所有读写走云函数：函数内由云开发自动注入的 openid 查成员身份与状态（是否在圈、是否在记录的可见范围内），通过后才执行数据库操作。三档可见性 + `pairIds` 快照 + 成员状态是复合逻辑，CloudBase 自定义安全规则的表达式写不了这种过滤，硬写必漏；云函数收口让鉴权逻辑集中一处、可调试，也为弱网上传策略（T7）提供统一入口。代价是每次操作多一跳——对 4–6 人的家庭圈量级可忽略。
